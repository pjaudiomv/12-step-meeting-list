!(function() {
  "use strict";
  function e(t2) {
    return e = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(e2) {
      return typeof e2;
    } : function(e2) {
      return e2 && "function" == typeof Symbol && e2.constructor === Symbol && e2 !== Symbol.prototype ? "symbol" : typeof e2;
    }, e(t2);
  }
  function t(e2, t2) {
    if (!(e2 instanceof t2)) throw new TypeError("Cannot call a class as a function");
  }
  function i(e2, t2) {
    for (var i2 = 0; i2 < t2.length; i2++) {
      var n2 = t2[i2];
      n2.enumerable = n2.enumerable || false, n2.configurable = true, "value" in n2 && (n2.writable = true), Object.defineProperty(e2, n2.key, n2);
    }
  }
  function n(e2, t2, i2) {
    return t2 in e2 ? Object.defineProperty(e2, t2, { value: i2, enumerable: true, configurable: true, writable: true }) : e2[t2] = i2, e2;
  }
  function r(e2, t2) {
    var i2 = Object.keys(e2);
    if (Object.getOwnPropertySymbols) {
      var n2 = Object.getOwnPropertySymbols(e2);
      t2 && (n2 = n2.filter((function(t3) {
        return Object.getOwnPropertyDescriptor(e2, t3).enumerable;
      }))), i2.push.apply(i2, n2);
    }
    return i2;
  }
  function s(e2) {
    for (var t2 = 1; t2 < arguments.length; t2++) {
      var i2 = null != arguments[t2] ? arguments[t2] : {};
      t2 % 2 ? r(Object(i2), true).forEach((function(t3) {
        n(e2, t3, i2[t3]);
      })) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e2, Object.getOwnPropertyDescriptors(i2)) : r(Object(i2)).forEach((function(t3) {
        Object.defineProperty(e2, t3, Object.getOwnPropertyDescriptor(i2, t3));
      }));
    }
    return e2;
  }
  function a(e2, t2) {
    (null == t2 || t2 > e2.length) && (t2 = e2.length);
    for (var i2 = 0, n2 = new Array(t2); i2 < t2; i2++) n2[i2] = e2[i2];
    return n2;
  }
  function l(e2, t2) {
    var i2;
    if ("undefined" == typeof Symbol || null == e2[Symbol.iterator]) {
      if (Array.isArray(e2) || (i2 = (function(e3, t3) {
        if (e3) {
          if ("string" == typeof e3) return a(e3, t3);
          var i3 = Object.prototype.toString.call(e3).slice(8, -1);
          return "Object" === i3 && e3.constructor && (i3 = e3.constructor.name), "Map" === i3 || "Set" === i3 ? Array.from(e3) : "Arguments" === i3 || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(i3) ? a(e3, t3) : void 0;
        }
      })(e2)) || t2 && e2 && "number" == typeof e2.length) {
        i2 && (e2 = i2);
        var n2 = 0, r2 = function() {
        };
        return { s: r2, n: function() {
          return n2 >= e2.length ? { done: true } : { done: false, value: e2[n2++] };
        }, e: function(e3) {
          throw e3;
        }, f: r2 };
      }
      throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
    }
    var s2, l2 = true, o2 = false;
    return { s: function() {
      i2 = e2[Symbol.iterator]();
    }, n: function() {
      var e3 = i2.next();
      return l2 = e3.done, e3;
    }, e: function(e3) {
      o2 = true, s2 = e3;
    }, f: function() {
      try {
        l2 || null == i2.return || i2.return();
      } finally {
        if (o2) throw s2;
      }
    } };
  }
  var o = 86400, u = { bubbles: true, cancelable: false, detail: null };
  function c(e2, t2) {
    return e2 == o && t2.show2400 ? e2 : e2 % o;
  }
  var m, d = { appendTo: "body", className: null, closeOnWindow: false, closeOnScroll: false, disableTextInput: false, disableTimeRanges: [], disableTouchKeyboard: false, durationTime: null, forceRoundTime: false, lang: {}, listWidth: null, maxTime: null, minTime: null, noneOption: false, orientation: "l", roundingFunction: function(e2, t2) {
    if (null === e2) return null;
    for (var i2 = 0, n2 = 0; n2 < e2; ) i2++, n2 += 60 * t2.step(i2);
    var r2 = n2 - 60 * t2.step(i2 - 1);
    return c(e2 - r2 < n2 - e2 ? r2 : n2, t2);
  }, scrollDefault: null, selectOnBlur: false, show2400: false, showDuration: false, showOn: ["click", "focus"], step: 30, stopScrollPropagation: false, timeFormat: "g:ia", typeaheadHighlight: true, useSelect: false, wrapHours: true }, f = { am: "am", pm: "pm", AM: "AM", PM: "PM", decimal: ".", mins: "mins", hr: "hr", hrs: "hrs" }, p = (function() {
    function n2(e2) {
      var i2 = arguments.length > 1 && void 0 !== arguments[1] ? arguments[1] : {};
      t(this, n2), this._handleFormatValue = this._handleFormatValue.bind(this), this._handleKeyUp = this._handleKeyUp.bind(this), this.targetEl = e2;
      var r3 = n2.extractAttrOptions(e2, Object.keys(d));
      this.settings = this.parseSettings(s(s(s({}, d), i2), r3));
    }
    var r2, a2, c2;
    return r2 = n2, c2 = [{ key: "extractAttrOptions", value: function(e2, t2) {
      var i2, n3 = {}, r3 = l(t2);
      try {
        for (r3.s(); !(i2 = r3.n()).done; ) {
          var s2 = i2.value;
          s2 in e2.dataset && (n3[s2] = e2.dataset[s2]);
        }
      } catch (e3) {
        r3.e(e3);
      } finally {
        r3.f();
      }
      return n3;
    } }, { key: "isVisible", value: function(e2) {
      var t2 = e2[0];
      return t2.offsetWidth > 0 && t2.offsetHeight > 0;
    } }, { key: "hideAll", value: function() {
      var e2, t2 = l(document.getElementsByClassName("ui-timepicker-input"));
      try {
        for (t2.s(); !(e2 = t2.n()).done; ) {
          var i2 = e2.value.timepickerObj;
          i2 && i2.hideMe();
        }
      } catch (e3) {
        t2.e(e3);
      } finally {
        t2.f();
      }
    } }], (a2 = [{ key: "hideMe", value: function() {
      if (this.settings.useSelect) this.targetEl.blur();
      else if (this.list && n2.isVisible(this.list)) {
        this.settings.selectOnBlur && this._selectValue(), this.list.hide();
        var e2 = new CustomEvent("hideTimepicker", u);
        this.targetEl.dispatchEvent(e2);
      }
    } }, { key: "_findRow", value: function(e2) {
      if (!e2 && 0 !== e2) return false;
      var t2 = false;
      return e2 = this.settings.roundingFunction(e2, this.settings), !!this.list && (this.list.find("li").each((function(i2, n3) {
        var r3 = parseInt(n3.dataset.time);
        if (!isNaN(r3)) return r3 == e2 ? (t2 = n3, false) : void 0;
      })), t2);
    } }, { key: "_hideKeyboard", value: function() {
      return (window.navigator.msMaxTouchPoints || "ontouchstart" in document) && this.settings.disableTouchKeyboard;
    } }, { key: "_setTimeValue", value: function(e2, t2) {
      if ("INPUT" === this.targetEl.nodeName) {
        null === e2 && "" == this.targetEl.value || (this.targetEl.value = e2);
        var i2 = this;
        i2.settings.useSelect && "select" != t2 && i2.list && i2.list.val(i2._roundAndFormatTime(i2.anytime2int(e2)));
      }
      var n3 = new CustomEvent("selectTime", u);
      if (this.selectedValue != e2) {
        this.selectedValue = e2;
        var r3 = new CustomEvent("changeTime", u), s2 = new CustomEvent("change", Object.assign(u, { detail: "timepicker" }));
        return "select" == t2 ? (this.targetEl.dispatchEvent(n3), this.targetEl.dispatchEvent(r3), this.targetEl.dispatchEvent(s2)) : -1 == ["error", "initial"].indexOf(t2) && this.targetEl.dispatchEvent(r3), true;
      }
      return -1 == ["error", "initial"].indexOf(t2) && this.targetEl.dispatchEvent(n3), false;
    } }, { key: "_getTimeValue", value: function() {
      return "INPUT" === this.targetEl.nodeName ? this.targetEl.value : this.selectedValue;
    } }, { key: "_selectValue", value: function() {
      var e2 = this;
      e2.settings;
      var t2 = e2.list.find(".ui-timepicker-selected");
      if (t2.hasClass("ui-timepicker-disabled")) return false;
      if (!t2.length) return true;
      var i2 = t2.get(0).dataset.time;
      if (i2) {
        var n3 = parseInt(i2);
        isNaN(n3) || (i2 = n3);
      }
      return null !== i2 && ("string" != typeof i2 && (i2 = e2._int2time(i2)), e2._setTimeValue(i2, "select")), true;
    } }, { key: "anytime2int", value: function(t2) {
      return "number" == typeof t2 ? t2 : "string" == typeof t2 ? this.time2int(t2) : "object" === e(t2) && t2 instanceof Date ? 3600 * t2.getHours() + 60 * t2.getMinutes() + t2.getSeconds() : "function" == typeof t2 ? t2() : null;
    } }, { key: "time2int", value: function(e2) {
      if ("" === e2 || null == e2) return null;
      if ("now" === e2) return this.anytime2int(/* @__PURE__ */ new Date());
      if ("string" != typeof e2) return e2;
      e2 = e2.toLowerCase().replace(/[\s\.]/g, ""), "am" !== this.settings.lang.am || "a" != e2.slice(-1) && "p" != e2.slice(-1) || (e2 += "m");
      var t2 = /^(([^0-9]*))?([0-9]?[0-9])(([0-5][0-9]))?(([0-5][0-9]))?(([^0-9]*))$/;
      e2.match(/\W/) && (t2 = /^(([^0-9]*))?([0-9]?[0-9])(\W+([0-5][0-9]?))?(\W+([0-5][0-9]))?(([^0-9]*))$/);
      var i2 = e2.match(t2);
      if (!i2) return null;
      var n3 = parseInt(1 * i2[3], 10), r3 = i2[2] || i2[9], s2 = this.parseMinuteString(i2[5]), a3 = 1 * i2[7] || 0;
      r3 || 2 != i2[3].length || "0" != i2[3][0] || (r3 = "am"), n3 > 24 && !s2 && (n3 = 1 * i2[3][0], s2 = this.parseMinuteString(i2[3][1]));
      var l2 = n3;
      if (n3 <= 12 && r3) {
        var u2 = (r3 = r3.trim()) == this.settings.lang.pm || r3 == this.settings.lang.PM;
        l2 = 12 == n3 ? u2 ? 12 : 0 : n3 + (u2 ? 12 : 0);
      } else if (3600 * n3 + 60 * s2 + a3 >= o + (this.settings.show2400 ? 1 : 0)) {
        if (false === this.settings.wrapHours) return null;
        l2 = n3 % 24;
      }
      var c3 = 3600 * l2 + 60 * s2 + a3;
      if (n3 < 12 && !r3 && this.settings._twelveHourTime && this.settings.scrollDefault()) {
        var m2 = c3 - this.settings.scrollDefault();
        m2 < 0 && m2 >= -43200 && (c3 = (c3 + 43200) % o);
      }
      return c3;
    } }, { key: "parseMinuteString", value: function(e2) {
      e2 || (e2 = 0);
      var t2 = 1;
      return 1 == e2.length && (t2 = 10), parseInt(e2) * t2 || 0;
    } }, { key: "intStringDateOrFunc2func", value: function(e2) {
      var t2 = this;
      return null == e2 ? function() {
        return null;
      } : "function" == typeof e2 ? function() {
        return t2.anytime2int(e2());
      } : function() {
        return t2.anytime2int(e2);
      };
    } }, { key: "parseSettings", value: function(e2) {
      if (e2.lang = s(s({}, f), e2.lang), this.settings = e2, e2.listWidth && (e2.listWidth = this.anytime2int(e2.listWidth)), e2.minTime = this.intStringDateOrFunc2func(e2.minTime), e2.maxTime = this.intStringDateOrFunc2func(e2.maxTime), e2.durationTime = this.intStringDateOrFunc2func(e2.durationTime), e2.scrollDefault ? e2.scrollDefault = this.intStringDateOrFunc2func(e2.scrollDefault) : e2.scrollDefault = e2.minTime, "string" == typeof e2.timeFormat && e2.timeFormat.match(/[gh]/) && (e2._twelveHourTime = true), "function" != typeof e2.step) {
        var t2 = e2.step;
        e2.step = function() {
          return t2;
        };
      }
      return e2.disableTimeRanges = this._parseDisableTimeRanges(e2.disableTimeRanges), e2.closeOnWindowScroll && !e2.closeOnScroll && (e2.closeOnScroll = e2.closeOnWindowScroll), true === e2.closeOnScroll && (e2.closeOnScroll = window.document), e2;
    } }, { key: "_parseDisableTimeRanges", value: function(e2) {
      if (!e2 || 0 == e2.length) return [];
      for (var t2 in e2) e2[t2] = [this.anytime2int(e2[t2][0]), this.anytime2int(e2[t2][1])];
      for (t2 = (e2 = e2.sort((function(e3, t3) {
        return e3[0] - t3[0];
      }))).length - 1; t2 > 0; t2--) e2[t2][0] <= e2[t2 - 1][1] && (e2[t2 - 1] = [Math.min(e2[t2][0], e2[t2 - 1][0]), Math.max(e2[t2][1], e2[t2 - 1][1])], e2.splice(t2, 1));
      return e2;
    } }, { key: "_disableTextInputHandler", value: function(e2) {
      switch (e2.keyCode) {
        case 13:
        case 9:
          return;
        default:
          e2.preventDefault();
      }
    } }, { key: "_int2duration", value: function(e2, t2) {
      e2 = Math.abs(e2);
      var i2, n3, r3 = Math.round(e2 / 60), s2 = [];
      return r3 < 60 ? s2 = [r3, this.settings.lang.mins] : (i2 = Math.floor(r3 / 60), n3 = r3 % 60, 30 == t2 && 30 == n3 && (i2 += this.settings.lang.decimal + 5), s2.push(i2), s2.push(1 == i2 ? this.settings.lang.hr : this.settings.lang.hrs), 30 != t2 && n3 && (s2.push(n3), s2.push(this.settings.lang.mins))), s2.join(" ");
    } }, { key: "_roundAndFormatTime", value: function(e2) {
      if (null !== (e2 = this.settings.roundingFunction(e2, this.settings))) return this._int2time(e2);
    } }, { key: "_int2time", value: function(e2) {
      if ("number" != typeof e2) return null;
      var t2 = parseInt(e2 % 60), i2 = parseInt(e2 / 60 % 60), n3 = parseInt(e2 / 3600 % 24), r3 = new Date(1970, 0, 2, n3, i2, t2, 0);
      if (isNaN(r3.getTime())) return null;
      if ("function" == typeof this.settings.timeFormat) return this.settings.timeFormat(r3);
      for (var s2, a3, l2 = "", u2 = 0; u2 < this.settings.timeFormat.length; u2++) switch (a3 = this.settings.timeFormat.charAt(u2)) {
        case "a":
          l2 += r3.getHours() > 11 ? this.settings.lang.pm : this.settings.lang.am;
          break;
        case "A":
          l2 += r3.getHours() > 11 ? this.settings.lang.PM : this.settings.lang.AM;
          break;
        case "g":
          l2 += 0 == (s2 = r3.getHours() % 12) ? "12" : s2;
          break;
        case "G":
          s2 = r3.getHours(), e2 === o && (s2 = this.settings.show2400 ? 24 : 0), l2 += s2;
          break;
        case "h":
          0 != (s2 = r3.getHours() % 12) && s2 < 10 && (s2 = "0" + s2), l2 += 0 === s2 ? "12" : s2;
          break;
        case "H":
          s2 = r3.getHours(), e2 === o && (s2 = this.settings.show2400 ? 24 : 0), l2 += s2 > 9 ? s2 : "0" + s2;
          break;
        case "i":
          l2 += (i2 = r3.getMinutes()) > 9 ? i2 : "0" + i2;
          break;
        case "s":
          l2 += (t2 = r3.getSeconds()) > 9 ? t2 : "0" + t2;
          break;
        case "\\":
          u2++, l2 += this.settings.timeFormat.charAt(u2);
          break;
        default:
          l2 += a3;
      }
      return l2;
    } }, { key: "_setSelected", value: function() {
      var e2 = this.list;
      e2.find("li").removeClass("ui-timepicker-selected");
      var t2 = this.anytime2int(this._getTimeValue());
      if (null !== t2) {
        var i2 = this._findRow(t2);
        if (i2) {
          var n3 = i2.getBoundingClientRect(), r3 = e2.get(0).getBoundingClientRect(), s2 = n3.top - r3.top;
          if (s2 + n3.height > r3.height || s2 < 0) {
            var a3 = e2.scrollTop() + (n3.top - r3.top) - n3.height;
            e2.scrollTop(a3);
          }
          var l2 = parseInt(i2.dataset.time);
          (this.settings.forceRoundTime || l2 === t2) && i2.classList.add("ui-timepicker-selected");
        }
      }
    } }, { key: "_isFocused", value: function(e2) {
      return e2 === document.activeElement;
    } }, { key: "_handleFormatValue", value: function(e2) {
      e2 && "timepicker" == e2.detail || this._formatValue(e2);
    } }, { key: "_formatValue", value: function(e2, t2) {
      if ("" !== this.targetEl.value) {
        if (!this._isFocused(this.targetEl) || e2 && "change" == e2.type) {
          var i2 = this.settings, n3 = this.anytime2int(this.targetEl.value);
          if (null !== n3) {
            var r3 = this._isTimeRangeError(n3, i2);
            if (i2.forceRoundTime) {
              var s2 = i2.roundingFunction(n3, i2);
              s2 != n3 && (n3 = s2, t2 = null);
            }
            var a3 = this._int2time(n3);
            if (r3) {
              this._setTimeValue(a3);
              var l2 = new CustomEvent("timeRangeError", u);
              this.targetEl.dispatchEvent(l2);
            } else this._setTimeValue(a3, t2);
          } else {
            var o2 = new CustomEvent("timeFormatError", u);
            this.targetEl.dispatchEvent(o2);
          }
        }
      } else this._setTimeValue(null, t2);
    } }, { key: "_isTimeRangeError", value: function(e2, t2) {
      if (null !== t2.minTime && null !== t2.maxTime && (e2 < t2.minTime() || e2 > t2.maxTime())) return true;
      var i2, n3 = l(t2.disableTimeRanges);
      try {
        for (n3.s(); !(i2 = n3.n()).done; ) {
          var r3 = i2.value;
          if (e2 >= r3[0] && e2 < r3[1]) return true;
        }
      } catch (e3) {
        n3.e(e3);
      } finally {
        n3.f();
      }
      return false;
    } }, { key: "_generateNoneElement", value: function(t2, i2) {
      var n3, r3, s2, a3;
      return "object" == e(t2) ? (n3 = t2.label, r3 = t2.className, s2 = t2.value) : "string" == typeof t2 ? (n3 = t2, s2 = "") : $.error("Invalid noneOption value"), i2 ? (a3 = document.createElement("option")).value = s2 : (a3 = document.createElement("li")).dataset.time = String(s2), a3.innerText = n3, a3.classList.add(r3), a3;
    } }, { key: "_handleKeyUp", value: function(e2) {
      var t2 = this;
      if (!this.list || !n2.isVisible(this.list) || this.settings.disableTextInput) return true;
      if ("paste" !== e2.type && "cut" !== e2.type) switch (e2.keyCode) {
        case 96:
        case 97:
        case 98:
        case 99:
        case 100:
        case 101:
        case 102:
        case 103:
        case 104:
        case 105:
        case 48:
        case 49:
        case 50:
        case 51:
        case 52:
        case 53:
        case 54:
        case 55:
        case 56:
        case 57:
        case 65:
        case 77:
        case 80:
        case 186:
        case 8:
        case 46:
          this.settings.typeaheadHighlight ? this._setSelected() : this.list.hide();
      }
      else setTimeout((function() {
        t2.settings.typeaheadHighlight ? t2._setSelected() : t2.list.hide();
      }), 0);
    } }]) && i(r2.prototype, a2), c2 && i(r2, c2), n2;
  })();
  function h(t2) {
    return Array.isArray(t2) ? t2.map(h) : true === t2 ? { label: "None", value: "" } : "object" === e(t2) ? t2 : { label: t2, value: "" };
  }
  function g(e2) {
    var t2 = document.createElement("option");
    return t2.value = e2.value || e2.label, e2.duration ? t2.appendChild(document.createTextNode(e2.label + " (" + e2.duration + ")")) : t2.appendChild(document.createTextNode(e2.label)), e2.disabled && (t2.disabled = true), t2;
  }
  function v(e2) {
    var t2 = document.createElement("li");
    if (t2.dataset.time = e2.value, e2.className && t2.classList.add(e2.className), t2.className = e2.className, t2.appendChild(document.createTextNode(e2.label)), e2.duration) {
      var i2 = document.createElement("span");
      i2.appendChild(document.createTextNode("(" + e2.duration + ")")), i2.classList.add("ui-timepicker-duration"), t2.appendChild(i2);
    }
    return e2.disabled && t2.classList.add("ui-timepicker-disabled"), t2;
  }
  function y(e2) {
    var t2, i2 = [].concat((function(e3) {
      if (!e3.noneOption) return [];
      var t3 = h(e3.noneOption);
      return Array.isArray(e3.noneOption) ? t3 : [t3];
    })(e2.settings), (function(e3) {
      var t3, i3, n3 = e3.settings, r3 = null !== (t3 = n3.minTime()) && void 0 !== t3 ? t3 : 0, s3 = null !== (i3 = n3.maxTime()) && void 0 !== i3 ? i3 : r3 + o - 1;
      s3 < r3 && (s3 += o), 86399 === s3 && "string" == typeof n3.timeFormat && n3.show2400 && (s3 = o);
      for (var a2 = [], u2 = r3, m2 = 0; u2 <= s3; m2++, u2 += 60 * n3.step(m2)) {
        var d2 = u2, f2 = e3._int2time(d2), p2 = d2 % o < 43200 ? "ui-timepicker-am" : "ui-timepicker-pm", h2 = { label: f2, value: c(d2, n3), className: p2 };
        if ((null !== n3.minTime() || null !== n3.durationTime()) && n3.showDuration) {
          var g2, v2 = null !== (g2 = n3.durationTime()) && void 0 !== g2 ? g2 : n3.minTime();
          v2 > u2 && (v2 -= o);
          var y2 = e3._int2duration(u2 - v2, n3.step());
          h2.duration = y2;
        }
        var b, k = l(n3.disableTimeRanges);
        try {
          for (k.s(); !(b = k.n()).done; ) {
            var T = b.value;
            if (d2 % o >= T[0] && d2 % o < T[1]) {
              h2.disabled = true;
              break;
            }
          }
        } catch (e4) {
          k.e(e4);
        } finally {
          k.f();
        }
        a2.push(h2);
      }
      return a2;
    })(e2));
    if (t2 = e2.settings.useSelect ? (function(e3, t3) {
      var i3 = document.createElement("select");
      i3.classList.add("ui-timepicker-select"), t3 && (i3.name = "ui-timepicker-" + t3);
      var n3, r3 = l(e3);
      try {
        for (r3.s(); !(n3 = r3.n()).done; ) {
          var s3 = g(n3.value);
          i3.appendChild(s3);
        }
      } catch (e4) {
        r3.e(e4);
      } finally {
        r3.f();
      }
      return i3;
    })(i2, e2.targetEl.name) : (function(e3) {
      var t3 = document.createElement("ul");
      t3.classList.add("ui-timepicker-list");
      var i3, n3 = l(e3);
      try {
        for (n3.s(); !(i3 = n3.n()).done; ) {
          var r3 = v(i3.value);
          t3.appendChild(r3);
        }
      } catch (e4) {
        n3.e(e4);
      } finally {
        n3.f();
      }
      var s3 = document.createElement("div");
      return s3.classList.add("ui-timepicker-wrapper"), s3.tabIndex = -1, s3.style.display = "none", s3.style.position = "absolute", s3.appendChild(t3), s3;
    })(i2), e2.settings.className) {
      var n2, r2 = l(e2.settings.className.split(" "));
      try {
        for (r2.s(); !(n2 = r2.n()).done; ) {
          var s2 = n2.value;
          t2.classList.add(s2);
        }
      } catch (e3) {
        r2.e(e3);
      } finally {
        r2.f();
      }
    }
    return !e2.settings.showDuration || null === e2.settings.minTime && null === e2.settings.durationTime || (t2.classList.add("ui-timepicker-with-duration"), t2.classList.add("ui-timepicker-step-" + e2.settings.step())), t2;
  }
  !(function() {
    if ("function" == typeof window.CustomEvent) return false;
    window.CustomEvent = function(e2, t2) {
      t2 || (t2 = {}), t2 = Object.assign(u, t2);
      var i2 = document.createEvent("CustomEvent");
      return i2.initCustomEvent(e2, t2.bubbles, t2.cancelable, t2.detail), i2;
    };
  })(), m = function(t2) {
    var i2 = { init: function(e2) {
      return this.each((function() {
        var r3 = t2(this), a2 = new p(this, e2), l2 = a2.settings;
        if (l2.lang, this.timepickerObj = a2, r3.addClass("ui-timepicker-input"), l2.useSelect) n2(r3);
        else {
          if (r3.prop("autocomplete", "off"), l2.showOn) for (var o2 in l2.showOn) r3.on(l2.showOn[o2] + ".timepicker", i2.show);
          r3.on("change.timepicker", a2._handleFormatValue), r3.on("keydown.timepicker", s2), r3.on("keyup.timepicker", a2._handleKeyUp), l2.disableTextInput && r3.on("keydown.timepicker", a2._disableTextInputHandler), r3.on("cut.timepicker", a2._handleKeyUp), r3.on("paste.timepicker", a2._handleKeyUp), a2._formatValue(null, "initial");
        }
      }));
    }, show: function(e2) {
      var i3 = t2(this), s3 = i3[0].timepickerObj, a2 = s3.settings;
      if (e2 && e2.preventDefault(), a2.useSelect) s3.list.trigger("focus");
      else {
        s3._hideKeyboard() && i3.trigger("blur");
        var l2 = s3.list;
        if (!i3.prop("readonly") && (n2(i3), l2 = s3.list, !p.isVisible(l2))) {
          i3.is("input") && (s3.selectedValue = i3.val()), s3._setSelected(), p.hideAll(), "number" == typeof a2.listWidth && l2.width(i3.outerWidth() * a2.listWidth), l2.show();
          var o2 = {};
          a2.orientation.match(/r/) ? o2.left = i3.offset().left + i3.outerWidth() - l2.outerWidth() + parseInt(l2.css("marginLeft").replace("px", ""), 10) : a2.orientation.match(/l/) ? o2.left = i3.offset().left + parseInt(l2.css("marginLeft").replace("px", ""), 10) : a2.orientation.match(/c/) && (o2.left = i3.offset().left + (i3.outerWidth() - l2.outerWidth()) / 2 + parseInt(l2.css("marginLeft").replace("px", ""), 10)), "t" == (a2.orientation.match(/t/) ? "t" : a2.orientation.match(/b/) ? "b" : i3.offset().top + i3.outerHeight(true) + l2.outerHeight() > t2(window).height() + t2(window).scrollTop() ? "t" : "b") ? (l2.addClass("ui-timepicker-positioned-top"), o2.top = i3.offset().top - l2.outerHeight() + parseInt(l2.css("marginTop").replace("px", ""), 10)) : (l2.removeClass("ui-timepicker-positioned-top"), o2.top = i3.offset().top + i3.outerHeight() + parseInt(l2.css("marginTop").replace("px", ""), 10)), l2.offset(o2);
          var u2 = l2.find(".ui-timepicker-selected");
          if (!u2.length) {
            var c2 = s3.anytime2int(s3._getTimeValue());
            null !== c2 ? u2 = t2(s3._findRow(c2)) : a2.scrollDefault() && (u2 = t2(s3._findRow(a2.scrollDefault())));
          }
          if (u2.length && !u2.hasClass("ui-timepicker-disabled") || (u2 = l2.find("li:not(.ui-timepicker-disabled):first")), u2 && u2.length) {
            var m2 = l2.scrollTop() + u2.position().top - u2.outerHeight();
            l2.scrollTop(m2);
          } else l2.scrollTop(0);
          return a2.stopScrollPropagation && t2(document).on("wheel.ui-timepicker", ".ui-timepicker-wrapper", (function(e3) {
            e3.preventDefault();
            var i4 = t2(this).scrollTop();
            t2(this).scrollTop(i4 + e3.originalEvent.deltaY);
          })), t2(document).on("mousedown.ui-timepicker", r2), window.addEventListener("resize", r2), a2.closeOnScroll && t2(a2.closeOnScroll).on("scroll.ui-timepicker", r2), i3.trigger("showTimepicker"), this;
        }
      }
    }, hide: function(e2) {
      var t3 = this[0].timepickerObj;
      return t3 && t3.hideMe(), p.hideAll(), this;
    }, option: function(i3, r3) {
      return "string" == typeof i3 && void 0 === r3 ? this[0].timepickerObj.settings[i3] : this.each((function() {
        var s3 = t2(this), a2 = s3[0].timepickerObj, l2 = a2.settings, o2 = a2.list;
        "object" == e(i3) ? l2 = t2.extend(l2, i3) : "string" == typeof i3 && (l2[i3] = r3), l2 = a2.parseSettings(l2), a2.settings = l2, a2._formatValue({ type: "change" }, "initial"), o2 && (o2.remove(), a2.list = null), l2.useSelect && n2(s3);
      }));
    }, getSecondsFromMidnight: function() {
      var e2 = this[0].timepickerObj;
      return e2.anytime2int(e2._getTimeValue());
    }, getTime: function(e2) {
      var t3 = this[0].timepickerObj, i3 = t3._getTimeValue();
      if (!i3) return null;
      var n3 = t3.anytime2int(i3);
      if (null === n3) return null;
      e2 || (e2 = /* @__PURE__ */ new Date());
      var r3 = new Date(e2);
      return r3.setHours(n3 / 3600), r3.setMinutes(n3 % 3600 / 60), r3.setSeconds(n3 % 60), r3.setMilliseconds(0), r3;
    }, isVisible: function() {
      var e2 = this[0].timepickerObj;
      return !!(e2 && e2.list && p.isVisible(e2.list));
    }, setTime: function(e2) {
      var t3 = this[0].timepickerObj, i3 = t3.settings, n3 = t3.anytime2int(e2);
      if (t3._isTimeRangeError(n3, i3)) {
        var r3 = new CustomEvent("timeRangeError", u);
        t3.targetEl.dispatchEvent(r3);
      }
      if (i3.forceRoundTime) var s3 = t3._roundAndFormatTime(n3);
      else s3 = t3._int2time(n3);
      return e2 && null === s3 && i3.noneOption && (s3 = e2), t3._setTimeValue(s3, "initial"), t3._formatValue({ type: "change" }, "initial"), t3 && t3.list && t3._setSelected(), this;
    }, remove: function() {
      var e2 = this;
      if (e2.hasClass("ui-timepicker-input")) {
        var t3 = e2[0].timepickerObj, i3 = t3.settings;
        return e2.removeAttr("autocomplete", "off"), e2.removeClass("ui-timepicker-input"), e2.removeData("timepicker-obj"), e2.off(".timepicker"), t3.list && t3.list.remove(), i3.useSelect && e2.show(), t3.list = null, this;
      }
    } };
    function n2(e2) {
      var n3 = e2[0].timepickerObj, r3 = n3.list, s3 = n3.settings;
      r3 && r3.length && (r3.remove(), n3.list = null);
      var a2 = t2(y(n3));
      if (r3 = s3.useSelect ? a2 : a2.children("ul"), a2.data("timepicker-input", e2), n3.list = a2, s3.useSelect) e2.val() && r3.val(n3._roundAndFormatTime(n3.anytime2int(e2.val()))), r3.on("focus", (function() {
        t2(this).data("timepicker-input").trigger("showTimepicker");
      })), r3.on("blur", (function() {
        t2(this).data("timepicker-input").trigger("hideTimepicker");
      })), r3.on("change", (function() {
        n3._setTimeValue(t2(this).val(), "select");
      })), n3._setTimeValue(r3.val(), "initial"), e2.hide().after(r3);
      else {
        var l2 = s3.appendTo;
        "string" == typeof l2 ? l2 = t2(l2) : "function" == typeof l2 && (l2 = l2(e2)), l2.append(a2), n3._setSelected(), r3.on("mousedown click", "li", (function(s4) {
          e2.off("focus.timepicker"), e2.on("focus.timepicker-ie-hack", (function() {
            e2.off("focus.timepicker-ie-hack"), e2.on("focus.timepicker", i2.show);
          })), n3._hideKeyboard() || e2[0].focus(), r3.find("li").removeClass("ui-timepicker-selected"), t2(this).addClass("ui-timepicker-selected"), n3._selectValue() && (e2.trigger("hideTimepicker"), r3.on("mouseup.timepicker click.timepicker", "li", (function(e3) {
            r3.off("mouseup.timepicker click.timepicker"), a2.hide();
          })));
        }));
      }
    }
    function r2(e2) {
      if ("focus" != e2.type || e2.target != window) {
        var i3 = t2(e2.target);
        i3.closest(".ui-timepicker-input").length || i3.closest(".ui-timepicker-wrapper").length || (p.hideAll(), t2(document).off(".ui-timepicker"), t2(window).off(".ui-timepicker"));
      }
    }
    function s2(e2) {
      var n3 = t2(this), r3 = n3[0].timepickerObj, s3 = r3.list;
      if (!s3 || !p.isVisible(s3)) {
        if (40 != e2.keyCode) return true;
        i2.show.call(n3.get(0)), s3 = r3.list, r3._hideKeyboard() || n3.trigger("focus");
      }
      switch (e2.keyCode) {
        case 13:
          return r3._selectValue() && (r3._formatValue({ type: "change" }), r3.hideMe()), e2.preventDefault(), false;
        case 38:
          var a2 = s3.find(".ui-timepicker-selected");
          return a2.length ? a2.is(":first-child") || (a2.removeClass("ui-timepicker-selected"), a2.prev().addClass("ui-timepicker-selected"), a2.prev().position().top < a2.outerHeight() && s3.scrollTop(s3.scrollTop() - a2.outerHeight())) : (s3.find("li").each((function(e3, i3) {
            if (t2(i3).position().top > 0) return a2 = t2(i3), false;
          })), a2.addClass("ui-timepicker-selected")), false;
        case 40:
          return 0 === (a2 = s3.find(".ui-timepicker-selected")).length ? (s3.find("li").each((function(e3, i3) {
            if (t2(i3).position().top > 0) return a2 = t2(i3), false;
          })), a2.addClass("ui-timepicker-selected")) : a2.is(":last-child") || (a2.removeClass("ui-timepicker-selected"), a2.next().addClass("ui-timepicker-selected"), a2.next().position().top + 2 * a2.outerHeight() > s3.outerHeight() && s3.scrollTop(s3.scrollTop() + a2.outerHeight())), false;
        case 27:
          s3.find("li").removeClass("ui-timepicker-selected"), r3.hideMe();
          break;
        case 9:
          r3.hideMe();
          break;
        default:
          return true;
      }
    }
    t2.fn.timepicker = function(n3) {
      return this.length ? i2[n3] ? this.hasClass("ui-timepicker-input") ? i2[n3].apply(this, Array.prototype.slice.call(arguments, 1)) : this : "object" !== e(n3) && n3 ? void t2.error("Method " + n3 + " does not exist on jQuery.timepicker") : i2.init.apply(this, arguments) : this;
    }, t2.fn.timepicker.defaults = d;
  }, "object" === ("undefined" == typeof exports ? "undefined" : e(exports)) && exports && "object" === ("undefined" == typeof module ? "undefined" : e(module)) && module && module.exports === exports ? m(require("jquery")) : "function" == typeof define && define.amd ? define(["jquery"], m) : m(jQuery);
})();
;
var infowindow, searchLocation, searchMarker, tsmlmap, markers = [], bounds, locationIcon, searchIcon;
function createMap(scrollwheel, locations, searchLocation2) {
  if (tsml.debug) console.log("createMap() locations", locations);
  if (!tsmlmap) {
    tsmlmap = L.map("map");
    L.tileLayer("https://{s}s.basemaps.cartocdn.com/rastertiles/voyager/{z}/{x}/{y}{r}.png", {
      maxZoom: 19,
      attribution: '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> &copy; <a href="https://carto.com/attributions">CARTO</a>'
    }).addTo(tsmlmap);
  }
  bounds = {
    north: false,
    south: false,
    east: false,
    west: false
  };
  locationIcon = window.btoa(
    '<?xml version="1.0" encoding="utf-8"?><svg viewBox="-1.1 -1.086 43.182 63.273" xmlns="http://www.w3.org/2000/svg"><path fill="#f76458" stroke="#b3382c" stroke-width="3" d="M20.5,0.5 c11.046,0,20,8.656,20,19.333c0,10.677-12.059,21.939-20,38.667c-5.619-14.433-20-27.989-20-38.667C0.5,9.156,9.454,0.5,20.5,0.5z"/></svg>'
  );
  searchIcon = window.btoa(
    '<?xml version="1.0" encoding="utf-8"?><svg viewBox="-1.1 -1.086 43.182 63.273" xmlns="http://www.w3.org/2000/svg"><path fill="#2c78b3" stroke="#2c52b3" stroke-width="3" d="M20.5,0.5 c11.046,0,20,8.656,20,19.333c0,10.677-12.059,21.939-20,38.667c-5.619-14.433-20-27.989-20-38.667C0.5,9.156,9.454,0.5,20.5,0.5z"/></svg>'
  );
  setMapMarkers(locations, searchLocation2);
}
function formatAddress(address, street_only) {
  if (!address) return "";
  address = address.split(", ");
  if (street_only) return address[0];
  if (address[address.length - 1] == "USA") {
    address.pop();
    var state_and_zip = address.pop();
    address[address.length - 1] += ", " + state_and_zip;
  }
  return address.join("<br>");
}
function formatLink(url, text, exclude) {
  if (!url) return text;
  if (location.search) {
    var query_pairs = location.search.substr(1).split("&");
    var new_query_pairs = [];
    for (var i = 0; i < query_pairs.length; i++) {
      var query_parts = query_pairs[i].split("=");
      if (query_parts[0] != exclude) new_query_pairs[new_query_pairs.length] = query_parts[0] + "=" + query_parts[1];
    }
    if (new_query_pairs.length) {
      url += (url.indexOf("?") == -1 ? "?" : "&") + new_query_pairs.join("&");
    }
  }
  return '<a href="' + url + '">' + text + "</a>";
}
function removeSearchMarker() {
  searchLocation = null;
  if (typeof searchMarker == "object" && searchMarker) {
    searchMarker.setMap(null);
    searchMarker = null;
  }
}
function setMapBounds() {
  if (markers.length > 1) {
    tsmlmap.fitBounds(
      [
        [bounds.south, bounds.west],
        [bounds.north, bounds.east]
      ],
      { padding: [10, 10] }
    );
  } else if (markers.length == 1) {
    if (markers[0].getPopup()) {
      tsmlmap.setZoom(16);
      tsmlmap.panTo([bounds.north + 25e-4, bounds.east]);
      markers[0].togglePopup();
    } else {
      tsmlmap.setZoom(16);
      tsmlmap.panTo([bounds.north, bounds.east]);
    }
  }
}
function setMapMarker(title, position, content) {
  if (!position.lat && !position.lng) return;
  var marker2;
  var html = document.createElement("div");
  html.className = "marker";
  html.style.backgroundImage = "url(data:image/svg+xml;base64," + locationIcon + ")";
  html.style.width = "26px";
  html.style.height = "38.4px";
  var icon = L.divIcon({ className: "marker", html, iconAnchor: [13, 38.4], popupAnchor: [0, -22] });
  marker2 = new L.marker(position, { icon }).addTo(tsmlmap);
  if (content) {
    var popup = new L.popup();
    popup.setContent(content);
    marker2.bindPopup(popup);
  }
  marker2.addTo(tsmlmap);
  return marker2;
}
function setMapMarkers(locations, searchLocation2) {
  if (markers.length) {
    for (var i = 0; i < markers.length; i++) {
      markers[i].remove();
    }
    markers = [];
  }
  removeSearchMarker();
  if (searchLocation2) {
    if (tsml.debug) console.log("setMapMarker() searchLocation", searchLocation2);
    setSearchMarker(searchLocation2);
  }
  var location_array = typeof locations === "object" ? Object.keys(locations).map(function(e) {
    return locations[e];
  }).sort(function(a, b) {
    return b.latitude - a.latitude;
  }) : [];
  for (var i = 0; i < location_array.length; i++) {
    var location2 = location_array[i];
    if (tsml.debug) console.log("setMapMarkers() location", location2);
    var content;
    if (location2.url && location2.formatted_address && !location2.approximate) {
      content = '<h3 class="notranslate">' + formatLink(location2.url, location2.name, "post_type") + '</h3><address class="notranslate">' + formatAddress(location2.formatted_address) + "</address>";
      if (location2.directions && location2.directions_url) {
        content += '<a href="' + location2.directions_url + '" class="btn btn-secondary w-100"><svg class="icon" width="1em" height="1em" viewBox="0 0 16 16" fill="currentColor" xmlns="http://www.w3.org/2000/svg"><path fill-rule="evenodd" d="M9.896 2.396a.5.5 0 0 0 0 .708l2.647 2.646-2.647 2.646a.5.5 0 1 0 .708.708l3-3a.5.5 0 0 0 0-.708l-3-3a.5.5 0 0 0-.708 0z"/><path fill-rule="evenodd" d="M13.25 5.75a.5.5 0 0 0-.5-.5h-6.5a2.5 2.5 0 0 0-2.5 2.5v5.5a.5.5 0 0 0 1 0v-5.5a1.5 1.5 0 0 1 1.5-1.5h6.5a.5.5 0 0 0 .5-.5z"/></svg>' + location2.directions + "</a>";
      }
      if (location2.meetings && location2.meetings.length) {
        var current_day = null;
        for (var j = 0; j < location2.meetings.length; j++) {
          var meeting = location2.meetings[j];
          if (current_day != meeting.day) {
            if (current_day) content += "</dl>";
            current_day = meeting.day;
            if (typeof tsml.days[current_day] !== "undefined") content += "<h5>" + tsml.days[current_day] + "</h5>";
            content += "<dl>";
          }
          content += "<dt>" + meeting.time + "</dt><dd>" + formatLink(meeting.url, meeting.name, "post_type") + "</dd>";
        }
        content += "</dl>";
      }
    }
    var position = {
      lat: parseFloat(location2.latitude),
      lng: parseFloat(location2.longitude)
    };
    var marker2 = setMapMarker(location2.name, position, content);
    if (typeof marker2 == "object" && marker2) {
      if (!bounds.north || position.lat > bounds.north) bounds.north = position.lat;
      if (!bounds.south || position.lat < bounds.south) bounds.south = position.lat;
      if (!bounds.east || position.lng > bounds.east) bounds.east = position.lng;
      if (!bounds.west || position.lng < bounds.west) bounds.west = position.lng;
      if (location2.approximate === "yes") marker2.remove();
    }
    if (tsml.debug) console.log("setMapMarkers() marker", marker2);
    markers.push(marker2);
  }
  setMapBounds();
}
function setSearchMarker(data) {
  removeSearchMarker();
  if (!data || !data.latitude) return;
  var html = document.createElement("div");
  html.className = "marker";
  html.style.backgroundImage = "url(data:image/svg+xml;base64," + searchIcon + ")";
  html.style.width = "26px";
  html.style.height = "38.4px";
  var icon = L.divIcon({ className: "marker", html, iconAnchor: [13, 38.4], popupAnchor: [0, -22] });
  marker = new L.marker([data.latitude, data.longitude], { icon }).addTo(tsmlmap);
}
;
jQuery(function($2) {
  function runImport() {
    $2.getJSON(tsml.ajaxurl + "?action=tsml_import", function(data) {
      var $progress = $2("body.tsml_meeting_page_import div#tsml_import_progress");
      var total = $progress.attr("data-total");
      var percentage = Math.floor((total - data.remaining) / total * 95) + 5 + "%";
      $progress.find(".progress-bar").css({ width: percentage }).text(percentage);
      var $counts = $2("#tsml_counts");
      var types = ["meetings", "locations", "groups", "regions"];
      for (var i = 0; i < types.length; i++) {
        var type = types[i];
        if (data.counts[type] > 0) {
          if ($counts.hasClass("hidden")) $counts.removeClass("hidden");
          $li = $counts.find("li." + type);
          if ($li.hasClass("hidden")) $li.removeClass("hidden");
          if ($li.text(data.descriptions[type])) ;
        }
      }
      if (data.data_sources) {
        $2.each(data.data_sources, function(url, props) {
          $2('tr[data-source="' + url + '"] td.count_meetings').text(props.count_meetings);
        });
      }
      if (data.errors.length) {
        $errors = $2("#tsml_import_errors");
        if ($errors.hasClass("hidden")) $errors.removeClass("hidden");
        for (var i = 0; i < data.errors.length; i++) $errors.append(data.errors[i]);
      }
      if (data.remaining) runImport();
    }).fail(function(jqxhr, textStatus, error) {
      console.warn(textStatus, error);
    });
  }
  if ($2("div#tsml_import_progress").length) {
    $2("div#tsml_import_progress div.progress-bar").css({ width: "5%" });
    $2("#tsml_import_errors").addClass("hidden");
    runImport();
  }
  const $filter = $2("#filter_type");
  if ($filter.length) {
    $filter.on("change", () => {
      const type = $filter.val();
      const url = new URL(window.location);
      if (type) {
        url.searchParams.set("filter_type", $filter.val());
      } else if (url.searchParams.has("filter_type")) {
        url.searchParams.delete("filter_type");
      }
      window.location = url.toString();
    });
  }
  $2("table form span").click(function() {
    const feedName = $2(this).parents("[data-source]").find("[data-source-name]").text().trim();
    if (confirm(`Remove the ${feedName} feed?`)) {
      $2(this).parent().submit();
    }
  });
  var $post_type = $2("input#post_type");
  if ($post_type.length && $post_type.val() == "tsml_meeting") {
    let updateFormState2 = function() {
      const pendingFields = [];
      const errorFields = [];
      Object.values($fields).forEach(($field) => {
        const state = $field.data("state");
        const el = $field[0];
        if ("loading" === state) {
          pendingFields.push(el);
        }
        if ("error" === state && el.errorMsg) {
          errorFields.push(el);
        }
      });
      form_valid = !pendingFields.length && !errorFields.length;
      $2("#publish").toggleClass("disabled", !form_valid);
      $form.errorMsg.toggle(!!errorFields.length);
      if (errorFields.length) {
        const $ul = $form.errorMsg.find("ul");
        $ul.html("");
        errorFields.forEach((errorField) => {
          if (errorField.errorMsg?.innerText) {
            const $li2 = $2(`<li>
                            <a href="#${errorField.id}">${errorField.errorMsg.innerText}</a>
                        </li>`);
            $ul.append($li2);
          }
        });
      }
    }, resetClasses2 = function() {
      $fields.formatted_address.removeClass("error");
      $fields.location.removeClass("warning");
      $fields.formatted_address.removeClass("warning");
    };
    var updateFormState = updateFormState2, resetClasses = resetClasses2;
    var form_valid = true;
    var $form = $2("form#post");
    $form.errorMsg = $2(".tsml-form-errors");
    var $fields = {
      day: $form.find("select#day"),
      time: $form.find("input#time"),
      end_time: $form.find("input#end_time"),
      types: $form.find('input[name="types[]"]'),
      conference_url: $form.find("input#conference_url"),
      conference_phone: $form.find("input#conference_phone"),
      in_person: $form.find("input[name=in_person]"),
      location: $form.find("input#location"),
      location_notes: $form.find("textarea[name=location_notes]"),
      formatted_address: $form.find("input#formatted_address"),
      approximate: $form.find("input#approximate"),
      latitude: $form.find("input[name=latitude]"),
      longitude: $form.find("input[name=longitude]"),
      timezone: $form.find("select[name=timezone]"),
      group: $form.find("input#group"),
      group_notes: $form.find("textarea[name=group_notes]"),
      group_status: $form.find('input[name="group_status"]'),
      region: $form.find("select[name=region]"),
      district: $form.find("select[name=district]"),
      website: $form.find("input[name=website]"),
      email: $form.find("input[name=email]"),
      phone: $form.find("input[name=phone]"),
      contact_1_name: $form.find("input[name=contact_1_name]"),
      contact_1_email: $form.find("input[name=contact_1_email]"),
      contact_1_phone: $form.find("input[name=contact_1_phone]"),
      contact_2_name: $form.find("input[name=contact_2_name]"),
      contact_2_email: $form.find("input[name=contact_2_email]"),
      contact_2_phone: $form.find("input[name=contact_2_phone]"),
      contact_3_name: $form.find("input[name=contact_3_name]"),
      contact_3_email: $form.find("input[name=contact_3_email]"),
      contact_3_phone: $form.find("input[name=contact_3_phone]"),
      mailing_address: $form.find("input[name=mailing_address]"),
      venmo: $form.find("input[name=venmo]"),
      square: $form.find("input[name=square]"),
      paypal: $form.find("input[name=paypal]"),
      homegroup_online: $form.find("input[name=homegroup_online]"),
      last_contact: $form.find("input[name=last_contact]")
    };
    $2.fn.setState = function(state, code) {
      const $field = $2(this);
      let errorMsg = null;
      $field.siblings("[data-message]").removeClass("show");
      $field.removeClass("error warning");
      if (-1 === ["warning", "loading", "error"].indexOf(state)) {
        state = "";
      }
      $field.data("state", state);
      if ("error" === state || "warning" === state) {
        $field.addClass(state);
        if (code) {
          let $error = $field.siblings("[data-message=" + code + "]");
          $error.addClass("show");
          errorMsg = $error[0] || null;
        }
      }
      $field[0].errorMsg = errorMsg;
      updateFormState2();
      return this;
    };
    $2.fn.clearState = function() {
      return $2(this).setState();
    };
    $form.submit(function() {
      return form_valid;
    });
    $2(".toggle_more").on("click", "a", function(e) {
      e.preventDefault();
      $2(this).closest(".checkboxes").toggleClass("showing_more");
    });
    $fields.day.on("change", function() {
      var val = $2(this).val();
      if (val) {
        $fields.time.removeAttr("disabled");
        $fields.end_time.removeAttr("disabled");
        if (!$fields.time.val()) {
          $fields.time.val("00:00").timepicker();
          $fields.end_time.val("01:00").timepicker();
        }
      } else {
        $fields.time.attr("data-value", $fields.time.val()).val("").attr("disabled", "disabled");
        $fields.end_time.attr("data-value", $fields.end_time.val()).val("").attr("disabled", "disabled");
      }
    });
    $2("input.time").timepicker();
    $fields.time.on("change", function() {
      var parts = $2(this).val().split(":");
      if (parts.length !== 2) return;
      var hours = parts[0] - 0;
      var parts = parts[1].split(" ");
      if (parts.length !== 2) return;
      var minutes = parts[0];
      var ampm = parts[1];
      if (hours == 12) {
        hours = 1;
      } else {
        hours++;
        if (hours == 12) {
          ampm = ampm == "am" ? "pm" : "am";
        }
      }
      hours += "";
      $fields.end_time.val(hours + ":" + minutes + " " + ampm);
    });
    $fields.types.on("change", function() {
      if ($fields.types.filter('[value="C"]').prop("checked") && $fields.types.filter('[value="O"]').prop("checked")) {
        if ($2(this).val() == "C") {
          $fields.types.filter('[value="O"]').prop("checked", false);
        } else {
          $fields.types.filter('[value="C"]').prop("checked", false);
        }
      }
    });
    $2.getJSON(tsml.ajaxurl + "?action=tsml_locations", function(data) {
      $fields.location.autocomplete({
        source: data,
        minLength: 1,
        select: function($e, selected) {
          var location2 = selected.item;
          if (tsml.debug) console.log("Location: ", location2);
          $fields.formatted_address.val(location2.formatted_address).trigger("change");
          $fields.latitude.val(location2.latitude);
          $fields.longitude.val(location2.longitude);
          $2("select[name=region] option[value=" + location2.region + "]").prop("selected", true);
          $fields.location_notes.val(location2.notes);
          $fields.timezone.val(location2.timezone);
        }
      });
    });
    $2.getJSON(tsml.ajaxurl + "?action=tsml_groups", function(data) {
      $fields.group.autocomplete({
        source: data,
        minLength: 1,
        select: function($e, selected) {
          var group = selected.item;
          if (tsml.debug) console.log("Selected: ", selected);
          $fields.district.val(group.district);
          $fields.website.val(group.website);
          $fields.email.val(group.email);
          $fields.phone.val(group.phone);
          $fields.contact_1_name.val(group.contact_1_name);
          $fields.contact_1_email.val(group.contact_1_email);
          $fields.contact_1_phone.val(group.contact_1_phone);
          $fields.contact_2_name.val(group.contact_2_name);
          $fields.contact_2_email.val(group.contact_2_email);
          $fields.contact_2_phone.val(group.contact_2_phone);
          $fields.contact_3_name.val(group.contact_3_name);
          $fields.contact_3_email.val(group.contact_3_email);
          $fields.contact_3_phone.val(group.contact_3_phone);
          $fields.mailing_address.val(group.mailing_address);
          $fields.venmo.val(group.venmo);
          $fields.last_contact.val(group.last_contact);
          $fields.group_notes.val(group.notes);
        }
      });
    });
    $fields.group_status.on("change", function() {
      $2("#contact-type").attr("data-type", $2(this).val());
      if ($2(this).val() == "meeting") {
        $fields.group.val("");
        $fields.group_notes.val("");
        $fields.district.val("");
        $2(".apply_group_to_location").addClass("hidden");
      }
    });
    $fields.group.on("change", function() {
      $2("div#group .apply_group_to_location").removeClass("hidden");
    });
    $fields.formatted_address.on("change", function() {
      $fields.formatted_address.setState("loading");
      $fields.latitude.val("");
      $fields.longitude.val("");
      var val = $2(this).val().trim();
      if (!val.length) {
        createMap(false);
        $fields.formatted_address.val("");
        if ($fields.in_person.filter('[value="yes"]').prop("checked")) {
          $fields.formatted_address.setState("error", 1);
        } else {
          $fields.formatted_address.setState("error", 3);
        }
        return;
      }
      $2.getJSON(
        tsml.ajaxurl,
        {
          action: "tsml_geocode",
          address: val,
          nonce: tsml.nonce
        },
        function(geocoded) {
          console.log("Geocoded: ", geocoded);
          if (geocoded.status == "error") {
            $fields.formatted_address.setState("error", 2);
            return;
          }
          $fields.latitude.val(geocoded.latitude);
          $fields.longitude.val(geocoded.longitude);
          createMap(false, { 0: geocoded });
          var region_id = false;
          if (!$2("select#region option[selected]").length) {
            $2("select#region option").each(function() {
              var region_name = $2(this).text().replace("&nbsp;", "").trim();
              if (geocoded.city && region_name == geocoded.city) {
                region_id = $2(this).attr("value");
              } else if (geocoded.formatted_address.indexOf(region_name) != -1) {
                region_id = $2(this).attr("value");
              }
            });
          }
          $fields.formatted_address.val(geocoded.formatted_address).trigger("keyup");
          $fields.approximate.val(geocoded.approximate);
          $2.getJSON(
            tsml.ajaxurl,
            {
              action: "tsml_address",
              formatted_address: geocoded.formatted_address
            },
            function(data) {
              if (data) {
                $fields.location.val(data.location);
                if (data.region != $fields.region.val()) {
                  $2("select[name=region] option").prop("selected", false);
                  $2("select[name=region] option[value=" + data.region + "]").prop("selected", true);
                }
                $fields.location_notes.val(data.location_notes);
              }
              if ((!data || !data.region) && !$2("select#region option[selected]").length && region_id) {
                $2("select[name=region] option[value=" + region_id + "]").prop("selected", true);
              }
              resetClasses2();
              meeting_is_online = $fields.conference_url.val() != "" || $fields.conference_phone.val() != "";
              if ($2("input[name=in_person]:checked").val() == "yes" && $fields.approximate.val() == "yes") {
                $fields.formatted_address.setState("error", 1);
              } else if ($2("input[name=in_person]:checked").val() == "no" && $fields.approximate.val() == "no" && meeting_is_online) {
                $fields.formatted_address.setState("warning", 4);
              } else {
                $fields.formatted_address.clearState();
              }
            }
          );
        }
      );
    }).on("keyup", function() {
      var original_address = $2(this).attr("data-original-value");
      if (original_address != $2(this).val()) {
        $fields.formatted_address.setState("loading");
      }
      if ($2("div.apply_address_to_location").length) {
        if (original_address.length && original_address != $2(this).val()) {
          $2("div.apply_address_to_location").removeClass("hidden");
        } else {
          $2("div.apply_address_to_location").addClass("hidden");
        }
      }
    });
    $fields.in_person.on("change", function() {
      $fields.formatted_address.trigger("change");
    });
    $fields.conference_url.validate = function() {
      $fields.conference_url.clearState();
      var conferenceUrl = decodeURI($fields.conference_url.val());
      if (conferenceUrl.match(/\bzoom\.us\b/i)) {
        let zoomFormat1 = conferenceUrl.match(/^(https?:\/\/)*([a-z0-9]+\.)*zoom\.us\/j\/(\d{8,20})(.*)$/i);
        let zoomFormat2 = conferenceUrl.match(/^(https?:\/\/)*([a-z0-9]+\.)*zoom\.us\/meeting\/register\/[a-z0-9\_]{10,}.*/i);
        if (zoomFormat1 || zoomFormat2) {
          let newConferenceUrl = conferenceUrl.replace(/^(https?:\/\/)*([a-z0-9]+\.)*zoom\.us/, "https://zoom.us");
          if (newConferenceUrl !== conferenceUrl) {
            $fields.conference_url.val(newConferenceUrl);
            $fields.conference_url.setState("warning", 2);
          }
        } else {
          $fields.conference_url.setState("error", 1);
          return;
        }
      }
    };
    if (tsml.tsml_debug) {
      $fields.conference_url.on("change", $fields.conference_url.validate);
      $fields.conference_url.validate();
    }
    $fields.conference_phone.on("change", function() {
      $fields.formatted_address.trigger("change");
    });
    $fields.paypal.validate = function() {
      var value = $fields.paypal.val().trim();
      if (value && !value.match(/^[a-z0-9]{1,20}$/i)) {
        $fields.paypal.setState("error", 1);
      } else {
        $fields.paypal.clearState();
      }
    };
    $fields.paypal.on("change", $fields.paypal.validate);
    $fields.paypal.validate();
    $fields.homegroup_online.validate = function() {
      var value = $fields.homegroup_online.val().trim();
      if (value && !value.match(/^[a-zA-Z0-9_-]*$/i)) {
        $fields.homegroup_online.setState("error", 1);
      } else {
        $fields.homegroup_online.clearState();
      }
    };
    $fields.homegroup_online.on("change", $fields.homegroup_online.validate);
    $fields.homegroup_online.validate();
    if (!$2("#auto_draft").val()) {
      $fields.formatted_address.trigger("change");
    }
  }
});
